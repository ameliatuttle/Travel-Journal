import SwiftUI

@main // This means that this is the entry point for the program. It functions as main and calls the other function
struct TravelPlannerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
